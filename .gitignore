# OS / editor cruft
Thumbs.db
.DS_Store
desktop.ini
*.swp
*~

# wrangler local cache/state
.wrangler/

# recovery working files (local MediaWiki render dir + php config); the source zip is kept
recovery/
raw/filemap.pre-recovery.json
